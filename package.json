{
  "name": "quickserve",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "gateway",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "turbo": "^2.10.9",
    "typescript": "^5.3.3"
  },
  "overrides": {
    "deepmerge-ts": "^8.0.1",
    "@prisma/config": {
      "deepmerge-ts": "^8.0.1"
    },
    "prisma": {
      "@prisma/config": {
        "deepmerge-ts": "^8.0.1"
      }
    }
  }
}